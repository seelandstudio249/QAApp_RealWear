# Python dependencies for AI model preparation (Phase 0)
# Compatible with Python 3.8 – 3.11

# SpeechBrain – provides the pre-trained ECAPA-TDNN model
# NOTE: speechbrain 1.x renamed the import; the export script handles both.
speechbrain>=0.5.15

# PyTorch – required by SpeechBrain and for ONNX export
# Install the CPU-only variant (smaller download, sufficient for export):
torch>=2.0.0

# ONNX – intermediate representation library
onnx>=1.14.0

# ONNX Runtime – used for quantisation and local verification
onnxruntime>=1.16.0

# NumPy – used by the verify_model.py script
numpy>=1.24.0
